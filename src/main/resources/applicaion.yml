spring:
  application:
    name: rehab_backend

  datasource:
    url: jdbc:mysql://rehab-rds.czsqck68o4qy.ap-northeast-2.rds.amazonaws.com:3306/mydb?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: admin
    password: wptelel!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      show_sql: true
  jwt:
    secret: "rehab-secret-key-wptelel!"
    access-token-validity-in-seconds: 3600      # 1시간
    refresh-token-validity-in-seconds: 1209600  # 14일

  data:
    redis:
      host: my-cache-server
      port: 6379

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
